var age = prompt("나이를 입력하세요", "만 나이로 입력합니다.");
var x,y;
var input;
input = prompt("정수를 입력하세요", "정수로");
x = parseInt(input);
input = prompt("정수를 입력하세요", "정수로");
y = parseInt(input);

document.write(x+y);